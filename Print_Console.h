#pragma once
#include <string>

// Function declarations for console printing and alerts
void displayAlert(const std::string& message);
void showVisualAlert();
void handleCriticalVital(const std::string& vitalName);
